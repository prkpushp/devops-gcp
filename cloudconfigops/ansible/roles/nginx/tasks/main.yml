- name: Install Nginx
  apt:
    name: nginx
    state: present

- name: Copy custom index page
  template:
    src: index.html.j2
    dest: /var/www/html/index.html
  notify:
    - restart nginx

- name: Ensure nginx service is running
  service:
    name: nginx
    enabled: yes
    state: started
