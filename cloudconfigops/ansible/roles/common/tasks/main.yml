- name: Update apt cache
  apt:
    update_cache: yes

- name: Install common packages
  apt:
    name:
      - curl
      - git
      - unzip
      - python3-pip
      - apt-transport-https
    state: present
